{
  "name": "manager-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities for local model service managers.",
  "main": "index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
