{
  "name": "local-ai-search-gateway",
  "version": "0.4.0",
  "private": true,
  "description": "Dockerized search-aware OpenAI-compatible gateway for local model services.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-isolation=none test/*.test.js"
  },
  "engines": {
    "node": ">=22.22.2"
  },
  "dependencies": {
    "@mozilla/readability": "0.6.0",
    "jsdom": "30.0.1",
    "pdfjs-dist": "6.2.108"
  }
}
